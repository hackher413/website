import { HoneycombHero } from "@/components/honeycomb";

/**
 * Temporary homepage — the immersive HoneycombHero fills the viewport. The full
 * homepage composition (stats band, about teaser, sponsors, CTA) is Step 6.
 */
export default function Home() {
  return <HoneycombHero />;
}
